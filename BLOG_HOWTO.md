# ブログの書き方（カーゴレックス ホームページ）

ブログは**専用の投稿画面**から書けます。スマホでもパソコンでも使えます。

- 投稿画面: https://konohito.github.io/cargolex-hp/admin.html
- 公開されるブログ: https://konohito.github.io/cargolex-hp/blog/

投稿画面はブックマークしておくと便利です（検索には出ないページです）。

---

## 最初に1回だけ：ログインキーの登録

投稿画面を開くと、最初に「ログインキー」の登録画面が出ます。画面の手順どおりに進めれば2分ほどで終わります。

**キーを作るときだけ、GitHubという英語のサイトが開きます。** 英語のままで正常です。
押す場所は投稿画面に日本語で書いてありますので、そのとおりに進めてください。

1. 画面の「**GitHubでキーを作る**」ボタンを押す（GitHubにログイン）
2. 開いたページで `Expiration`（有効期限）を `No expiration`（期限なし）にして、一番下の緑の `Generate token`（キーを作る）を押す
3. 出てきた `ghp_…` をコピーして、投稿画面に貼り付け「登録して始める」

英語の言葉の意味は、投稿画面の「英語の画面に出てくる言葉の意味」を開くと一覧で見られます。
すでにキーを受け取っている場合は、この手順は不要です。上の欄に貼り付けるだけで始められます。

**この作業は一度きり**です。2回目からは投稿画面を開くだけですぐ書けます。

登録したキーは**その端末のブラウザにだけ**保存されます。
スマホとパソコンの両方で書く場合は、それぞれで1回ずつ登録してください。
キーを消したいときは、投稿画面の右上「サインアウト」を押します。

> **記事を書く方がGitHubを触りたくない場合**
> 詳しい方が最初に一度、その方の端末（スマホなど）で上記の登録をしておけば、
> 以降はGitHubを一切開かずに投稿画面だけで運用できます。

---

## 記事を書く

投稿画面の「**記事を書く**」タブで入力します。右側にでき上がりのプレビューが出ます。

| 欄 | 内容 |
| --- | --- |
| タイトル | 記事の題名（必須） |
| 日付 | 記事の日付（初期値は今日） |
| カテゴリ | お庭の草刈・剪定／お引越し／お知らせ など。欄をタップすると候補が出ます |
| 写真 | 何枚でも追加できます。スマホの写真そのままでOK（自動で軽くします） |
| 本文 | 記事の中身 |

「記事のURL名」は「詳しい設定」の中にありますが、ふだんは触らなくて大丈夫です（自動でつきます）。

書けたら「**この内容で公開する**」を押します。1〜2分でホームページに反映されます。

書きかけの内容は自動で一時保存されるので、間違って画面を閉じても復元されます。

### 本文の書き方

ボタンを使うか、次の記号で書きます。

```
## 小見出し
### さらに小さい見出し

ふつうの文章。空の行を1つ入れると段落が分かれます。

- 箇条書き
- 箇条書き

1. 番号つき
2. 番号つき

**太字にしたい文字**

[リンクの文字](https://example.com/)

> 引用や補足に使えます

---
（横線）
```

### 写真の入れ方

「**写真を選ぶ**」を押して写真を選びます（**複数まとめて選べます**）。
パソコンなら、写真をそのまま枠へドラッグ＆ドロップしたり、コピーした画像を貼り付けることもできます。

追加した写真は一覧で並び、それぞれに次のボタンが付きます。

- **アイキャッチにする** … ブログ一覧と記事の先頭に大きく出す写真にします（1枚目が自動で設定されます）
- **本文に入れる** … 本文のカーソル位置に写真を差し込みます
- **削除** … 写真を取り消します（本文に入れていた場合もあわせて消えます）

実際のアップロードは「公開する」を押したときにまとめて行われます。
写真は自動で軽くしてから送るので、通信量や表示速度の心配はいりません。

---

## 書き直す・消す

「**記事の管理**」タブに、投稿済みの記事が新しい順に並びます。

- **書き直す**: 内容が投稿画面に読み込まれるので、直して「この内容で更新する」を押します
- **削除**: 確認のうえ削除します

どちらも1〜2分でホームページに反映されます。

---

## 困ったときは

| 表示 | 対処 |
| --- | --- |
| 接続キーが正しくない／期限切れ | 右上「サインアウト」→ 接続キーを作り直して登録 |
| 権限が足りません | 接続キーの `Contents` が `Read and write` か確認 |
| リポジトリにアクセスできません | 接続キーの対象に `konohito/cargolex-hp` が入っているか確認 |
| 公開したのに反映されない | 1〜2分待つ。https://github.com/konohito/cargolex-hp/actions で状況を確認 |

投稿画面が使えないときは、GitHubの画面から直接ファイルを追加することもできます
（`blog/posts/` に `2026-09-10-記事名.md` の形で作成）。

わからないことがあれば、Claudeのチャットに本文を送っていただければ代わりに投稿できます。

---

## 仕組み（技術メモ）

- 記事の実体は `blog/posts/*.md`。投稿画面はGitHubのAPI経由でこのファイルを作成・更新・削除します
- `main` への変更をきっかけに GitHub Actions が `tools/build_blog.py` を実行し、
  一覧・記事ページ・RSS・sitemap・トップページの最新記事を生成して公開します
- プレビューの表示（`js/admin.js`）と本番の生成（`tools/build_blog.py`）は同じ記法・同じ出力になるよう揃えています
